[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
rootDirectory = "relayer"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
